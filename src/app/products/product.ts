export interface ProductInterface{
    productId: number;
    productName: string;
    productCode: string;
    releaseDate: string;
    description: string;
    price: number;
    starRating: number;
    imageUrl: string;
}

//if we want to implement a method then we would have used a class , defined the properties in the constructoe and then write the method. 
